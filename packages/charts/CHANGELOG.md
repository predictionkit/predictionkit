# @prediction-kit/charts

## 0.2.0

### Minor Changes

- Initial release. `<ProbabilityChart>` renders a Recharts area chart of a market's Yes probability over time, accepting `data` directly or a `marketId` + `interval` (fetched via `usePriceHistory`).

### Patch Changes

- Updated dependencies
  - @prediction-kit/core@0.2.0
  - @prediction-kit/react@0.2.0
